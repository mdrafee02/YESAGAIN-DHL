services:
  - type: web
    name: yesagain-dhl-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --timeout 600 --workers 1 api:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0